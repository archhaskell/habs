# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=hs2dot
pkgrel=1
pkgver=0.1.4
pkgdesc="Generate graphviz-code from Haskell-code."
url="http://hackage.haskell.org/package/hs2dot"
license=('custom:OtherLicense')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-directory' 'haskell-haskell-src' 'haskell-haskell-src-exts' 'haskell-haskell98' 'haskell-split>=0.1.2')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/hs2dot/0.1.4/hs2dot-0.1.4.tar.gz)
md5sums=('040ec7562d59eb17f523a7d198122d3b')
build() {
    cd ${srcdir}/hs2dot-0.1.4
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 COPYING.txt ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
