# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=birdpp
pkgrel=2
pkgver=1.0
pkgdesc="A preprocessor for Bird-style Literate Haskell comments with Haddock markup."
url="http://hackage.haskell.org/package/BirdPP"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-haskell98')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/BirdPP/1.0/BirdPP-1.0.tar.gz)
md5sums=('091b1a15c5aa70776db4289b36f231a7')
build() {
    cd ${srcdir}/BirdPP-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
