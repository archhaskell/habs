# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=estreps
pkgrel=2
pkgver=0.3.1
pkgdesc="Repeats from ESTs"
url="http://hackage.haskell.org/package/estreps"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-bio>=0.4' 'haskell-bytestring' 'haskell-containers' 'haskell-random')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/estreps/0.3.1/estreps-0.3.1.tar.gz)
md5sums=('83ec0756b8fb5f4fc69a96af2b286c7a')
build() {
    cd ${srcdir}/estreps-0.3.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
