# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hmpf
pkgrel=2
pkgver=0.1
pkgdesc="An MPD client designed for a Home Theatre PC"
url="http://hackage.haskell.org/package/Hmpf"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-configfile' 'haskell-crypto' 'haskell-http' 'haskell-mtl' 'haskell-network' 'haskell-time' 'haskell-unix' 'haskell-utf8-string')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/Hmpf/0.1/Hmpf-0.1.tar.gz)
md5sums=('6c9e36546ef1c940749d233dfbf7e85d')
build() {
    cd ${srcdir}/Hmpf-0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
