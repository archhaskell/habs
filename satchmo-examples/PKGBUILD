# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=satchmo-examples
pkgrel=2
pkgver=1.4.1
pkgdesc="examples that show how to use satchmo"
url="http://hackage.haskell.org/package/satchmo-examples"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array' 'haskell-containers' 'haskell-process' 'haskell-satchmo>=1.4' 'haskell-satchmo-backends>=1.4' 'haskell-satchmo-funsat>=1.4')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/satchmo-examples/1.4.1/satchmo-examples-1.4.1.tar.gz)
md5sums=('73721664af9b99461294ac403a6210e2')
build() {
    cd ${srcdir}/satchmo-examples-1.4.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
