# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=make-hard-links
pkgrel=2
pkgver=0.2
pkgdesc="Change duplicated files into hard-links."
url="http://hackage.haskell.org/package/make-hard-links"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-bytestring' 'haskell-containers' 'haskell-directory' 'haskell-filepath' 'haskell-unix')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/make-hard-links/0.2/make-hard-links-0.2.tar.gz)
md5sums=('4ddb864fb5d65742be2fc4d8db3d3467')
build() {
    cd ${srcdir}/make-hard-links-0.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
