# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=emping
pkgrel=2
pkgver=0.6
pkgdesc="derives heuristic rules from nominal data"
url="http://hackage.haskell.org/package/Emping"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'gtk2hs' 'haskell-array' 'haskell-containers' 'haskell-fgl' 'haskell-mtl' 'haskell-parsec')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/Emping/0.6/Emping-0.6.tar.gz)
md5sums=('10f1f45aa21c142e6e61dff37cdafbe0')
build() {
    cd ${srcdir}/Emping-0.6
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
