# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=detrospector
pkgrel=1
pkgver=0.1
pkgdesc="Markov chain text generator"
url="http://hackage.haskell.org/package/detrospector"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-binary>=0.5' 'haskell-bytestring>=0.9' 'haskell-cmdargs>=0.6' 'haskell-containers>=0.3' 'haskell-hashable>=1.0' 'haskell-hashmap>=1.1' 'haskell-mwc-random>=0.8' 'haskell-text>=0.8' 'haskell-zlib>=0.5')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/detrospector/0.1/detrospector-0.1.tar.gz)
md5sums=('a97d85224b6bb47b55aea30ef736b1b6')
build() {
    cd ${srcdir}/detrospector-0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
