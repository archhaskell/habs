# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hunp
pkgrel=2
pkgver=0.1
pkgdesc="Unpacker tool with DWIM"
url="http://hackage.haskell.org/package/hunp"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-bytestring' 'haskell-directory' 'haskell-filepath' 'haskell-monads-fd' 'haskell-parsec>=3' 'haskell-pcre-light' 'haskell-process' 'haskell-split')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/hunp/0.1/hunp-0.1.tar.gz)
md5sums=('a979c90249c87fed7dc43b88022eb882')
build() {
    cd ${srcdir}/hunp-0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
