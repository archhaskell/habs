# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hsclock
pkgrel=2
pkgver=1.0
pkgdesc="An elegant analog clock using Haskell, GTK and Cairo"
url="http://hackage.haskell.org/package/hsclock"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'gtk2hs' 'haskell-old-time')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/hsclock/1.0/hsclock-1.0.tar.gz)
md5sums=('1b8b100f8729e8e47fe99bd18632e8d4')
build() {
    cd ${srcdir}/hsclock-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
