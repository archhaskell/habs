# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=foo
pkgrel=2
pkgver=1.0
pkgdesc="Paper soccer, an OpenGL game."
url="http://hackage.haskell.org/package/foo"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-glut' 'haskell-opengl' 'haskell-containers' 'haskell-haskell98')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/foo/1.0/foo-1.0.tar.gz)
md5sums=('1dbe5ca46dbdf78b375ef6034dfed7d1')
build() {
    cd ${srcdir}/foo-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
