# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=darcs-graph
pkgrel=2
pkgver=1.0
pkgdesc="Generate graphs of darcs repository activity"
url="http://hackage.haskell.org/package/darcs-graph"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-bytestring' 'haskell-containers' 'haskell-directory' 'haskell-old-locale' 'haskell-old-time' 'haskell-process' 'haskell-time')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/darcs-graph/1.0/darcs-graph-1.0.tar.gz)
md5sums=('86008649edef15f1014d3b0c67164d40')
build() {
    cd ${srcdir}/darcs-graph-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
