# Maintainer: Arch Haskell Team <arch-haskell@haskell.org>
_hkgname=panda
pkgname=haskell-panda
pkgver=2009.4.1
pkgrel=2
pkgdesc="A simple static blog engine"
url="http://hackage.haskell.org/package/${_hkgname}"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=()
depends=('ghc' 'haskell-missingh' 'haskell-cgi' 'haskell-containers' 'haskell-data-default>=0.2' 'haskell-directory' 'haskell-filepath' 'haskell-gravatar>=0.3' 'haskell-haskell98' 'haskell-hcheat>=2008.11.6' 'haskell-kibro>=0.4.2' 'haskell-mps>=2008.11.6' 'haskell-network' 'haskell-old-locale' 'haskell-old-time' 'haskell-pandoc' 'haskell-parsec>=2' 'haskell-parsedate>=3000.0.0' 'haskell-process' 'haskell-rss>=3000.0.1' 'haskell-utf8-string>=0.3.3' 'haskell-xhtml')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/${_hkgname}/${pkgver}/${_hkgname}-${pkgver}.tar.gz)
install=${pkgname}.install
build() {
    cd ${srcdir}/${_hkgname}-${pkgver}
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} -O --enable-split-objs
    runhaskell Setup build
    runhaskell Setup haddock
    runhaskell Setup register   --gen-script
    runhaskell Setup unregister --gen-script
    sed -i -r -e "s|ghc-pkg.*unregister[^ ]* |&'--force' |" unregister.sh
}
package() {
    cd ${srcdir}/${_hkgname}-${pkgver}
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/${pkgname}/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/${pkgname}/unregister.sh
    install -d -m755 ${pkgdir}/usr/share/doc/ghc/html/libraries
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/${_hkgname}
    runhaskell Setup copy --destdir=${pkgdir}
}
md5sums=('26c7cccf1f1a850fd3a90b275461b966')
