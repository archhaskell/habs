# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=cabal2spec
pkgrel=2
pkgver=1.0
pkgdesc="Generates RPM Spec files from cabal files"
url="http://hackage.haskell.org/package/cabal2spec"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-cabal' 'haskell-unixutils' 'haskell-bytestring' 'haskell-directory' 'haskell-filepath' 'haskell-haskell98' 'haskell-old-locale' 'haskell-process' 'haskell-tar' 'haskell-time' 'haskell-unix' 'haskell-zlib')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/cabal2spec/1.0/cabal2spec-1.0.tar.gz)
md5sums=('b1a7cae633a05b092f77a56845a5a348')
build() {
    cd ${srcdir}/cabal2spec-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
