# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=lambdabot
pkgrel=2
pkgver=4.2.2.1
pkgdesc="Lambdabot is a development tool and advanced IRC bot"
url="http://hackage.haskell.org/package/lambdabot"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=()
depends=('gmp' 'ghc' 'haskell-cabal' 'haskell-array' 'haskell-binary>0.2' 'haskell-brainfuck' 'haskell-containers' 'haskell-directory' 'haskell-haskell-src' 'haskell-haskell-src-exts' 'haskell-lambdabot-utils' 'haskell-network' 'haskell-oeis' 'haskell-old-time' 'haskell-parsec' 'haskell-pretty' 'haskell-random' 'haskell-readline' 'haskell-regex-compat' 'haskell-show>=0.3' 'haskell-template-haskell' 'haskell-unlambda' 'haskell-utf8-string')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/lambdabot/4.2.2.1/lambdabot-4.2.2.1.tar.gz)
install=lambdabot.install
md5sums=('02cb56d791b6856d7682e9dbdc82303f')
build() {
    cd ${srcdir}/lambdabot-4.2.2.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup haddock || return 1
    runhaskell Setup register   --gen-script || return 1
    runhaskell Setup unregister --gen-script || return 1
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/$pkgname/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/$pkgname/unregister.sh
    install -d -m755 $pkgdir/usr/share/doc/ghc/html/libraries
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/lambdabot
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
