# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=rbr
pkgrel=2
pkgver=0.8.5
pkgdesc="Mask nucleotide (EST) sequences in Fasta format"
url="http://hackage.haskell.org/package/rbr"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-bio>=0.4' 'haskell-bytestring' 'haskell-containers')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/rbr/0.8.5/rbr-0.8.5.tar.gz)
md5sums=('fba4c6cd48bafef60464d4938d383389')
build() {
    cd ${srcdir}/rbr-0.8.5
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
