# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=memscript
pkgrel=2
pkgver=0.0.0.2
pkgdesc="Command line utility for memorizing scriptures or any other text"
url="http://hackage.haskell.org/package/memscript"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-haskell98' 'haskell-readline' 'haskell-utf8-string')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/memscript/0.0.0.2/memscript-0.0.0.2.tar.gz)
md5sums=('32f5bb37b3ba1a69c716bb02353d1d6a')
build() {
    cd ${srcdir}/memscript-0.0.0.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
