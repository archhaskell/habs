# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=riot
pkgrel=2
pkgver=1.20080618
pkgdesc="Riot is an Information Organisation Tool"
url="http://hackage.haskell.org/package/riot"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-containers' 'haskell-directory' 'haskell-haskell98' 'haskell-mtl' 'haskell-old-locale' 'haskell-packedstring' 'haskell-process' 'haskell-unix' 'ncursesw')
depends=('gmp' 'ncursesw')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/riot/1.20080618/riot-1.20080618.tar.gz)
md5sums=('b00d79c8990a8134bc2ffffe8c1b361f')
build() {
    cd ${srcdir}/riot-1.20080618
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
