# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=haskell-hermes
pkgrel=1
pkgver=0.0.4
pkgdesc="Message-based middleware layer"
url="http://hackage.haskell.org/package/Hermes"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=()
depends=('ghc' 'haskell-cabal' 'haskell-aes>=0.2.4' 'haskell-rsa>=1.0.2' 'haskell-sha2>=0.1.1' 'haskell-bytestring' 'haskell-cereal>=0.2' 'haskell-containers' 'haskell-hslogger>=1.0.7' 'haskell-monads-tf' 'haskell-network' 'haskell-old-time' 'haskell-random' 'haskell-random-shuffle' 'haskell-stm' 'haskell-syb' 'haskell-time>=1.1.4' 'haskell-transformers>=0.1.4.0' 'haskell-unamb>=0.2.2' 'haskell-yjtools>=0.9.7')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/Hermes/0.0.4/Hermes-0.0.4.tar.gz)
install=haskell-hermes.install
md5sums=('8cb97c60441cac5d604323201edb5673')
build() {
    cd ${srcdir}/Hermes-0.0.4
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup haddock || return 1
    runhaskell Setup register   --gen-script || return 1
    runhaskell Setup unregister --gen-script || return 1
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/$pkgname/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/$pkgname/unregister.sh
    install -d -m755 $pkgdir/usr/share/doc/ghc/html/libraries
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/Hermes
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 COPYING ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
