# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=sgdemo
pkgrel=2
pkgver=1.1
pkgdesc="An example of using the SG and OpenGL libraries"
url="http://hackage.haskell.org/package/SGdemo"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-glut' 'haskell-opengl' 'haskell-sg')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/SGdemo/1.1/SGdemo-1.1.tar.gz)
md5sums=('a009f7e1d50774164e7171727f16585b')
build() {
    cd ${srcdir}/SGdemo-1.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
