# Maintainer: Arch Haskell Team <arch-haskell@haskell.org>
_hkgname=snap-server
pkgname=haskell-snap-server
pkgver=0.2.15.1
pkgrel=1
pkgdesc="A fast, iteratee-based, epoll-enabled web server for the Snap Framework"
url="http://hackage.haskell.org/package/${_hkgname}"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=()
depends=('ghc' 'haskell-psqueue<1.2' 'haskell-array<0.4' 'haskell-attoparsec<0.9' 'haskell-attoparsec-iteratee<0.2' 'haskell-bytestring' 'haskell-bytestring-nums' 'haskell-bytestring-show<0.4' 'haskell-cereal<0.4' 'haskell-containers' 'haskell-directory-tree' 'haskell-dlist<0.6' 'haskell-filepath' 'haskell-iteratee<0.4' 'haskell-monads-fd<0.1.0.3' 'haskell-murmur-hash<0.2' 'haskell-network<2.2.2' 'haskell-network-bytestring<0.2' 'haskell-old-locale' 'haskell-snap-core<=0.2.15.1' 'haskell-template-haskell' 'haskell-time' 'haskell-transformers' 'haskell-unix' 'haskell-unix-compat' 'haskell-vector<0.8')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/${_hkgname}/${pkgver}/${_hkgname}-${pkgver}.tar.gz)
install=${pkgname}.install
build() {
    cd ${srcdir}/${_hkgname}-${pkgver}
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} -O --enable-split-objs
    runhaskell Setup build
    runhaskell Setup haddock
    runhaskell Setup register   --gen-script
    runhaskell Setup unregister --gen-script
    sed -i -r -e "s|ghc-pkg.*unregister[^ ]* |&'--force' |" unregister.sh
}
package() {
    cd ${srcdir}/${_hkgname}-${pkgver}
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/${pkgname}/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/${pkgname}/unregister.sh
    install -d -m755 ${pkgdir}/usr/share/doc/ghc/html/libraries
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/${_hkgname}
    runhaskell Setup copy --destdir=${pkgdir}
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/${pkgname}/LICENSE
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
md5sums=('cf67974f611caa9e8cca94891de16154')
