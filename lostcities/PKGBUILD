# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=lostcities
pkgrel=2
pkgver=0.2
pkgdesc="An implementation of an adictive two-player card game"
url="http://hackage.haskell.org/package/lostcities"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array' 'haskell-containers' 'haskell-haskell98' 'haskell-mtl' 'haskell-wx>=0.11' 'haskell-wxcore>=0.11')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/lostcities/0.2/lostcities-0.2.tar.gz)
md5sums=('f5cbf61bc6a6656219051f92bf865e15')
build() {
    cd ${srcdir}/lostcities-0.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
