# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=pdf2line
pkgrel=2
pkgver=0.0.1
pkgdesc="Simple command-line utility to convert PDF into text"
url="http://hackage.haskell.org/package/pdf2line"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-findbin' 'haskell-binary' 'haskell-bytestring' 'haskell-containers' 'haskell-filepath' 'haskell-process')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/pdf2line/0.0.1/pdf2line-0.0.1.tar.gz)
md5sums=('7ebf9db25a1ef136475396eedeff6f66')
build() {
    cd ${srcdir}/pdf2line-0.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
