# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=ehaskell
pkgrel=2
pkgver=0.7
pkgdesc="like eruby, ehaskell is embedded haskell."
url="http://hackage.haskell.org/package/ehaskell"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-directory' 'haskell-filepath' 'haskell-mtlparse>=0.0.1' 'haskell-process' 'haskell-regexpr>=0.3.3' 'haskell-utf8-string' 'haskell-yjtools>=0.8')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/ehaskell/0.7/ehaskell-0.7.tar.gz)
md5sums=('487e42dff8e0d4f88f27942216b063b0')
build() {
    cd ${srcdir}/ehaskell-0.7
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
