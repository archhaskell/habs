# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=sat-micro-hs
pkgrel=2
pkgver=0.1.1
pkgdesc="A minimal SAT solver"
url="http://hackage.haskell.org/package/sat-micro-hs"
license=('LGPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-containers' 'haskell-haskell98' 'haskell-mtl' 'haskell-parse-dimacs' 'haskell-pretty')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/sat-micro-hs/0.1.1/sat-micro-hs-0.1.1.tar.gz)
md5sums=('6b7df147df8ec71cd28149a0d61bb602')
build() {
    cd ${srcdir}/sat-micro-hs-0.1.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
