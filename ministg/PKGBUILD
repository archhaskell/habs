# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=ministg
pkgrel=2
pkgver=0.2
pkgdesc="an interpreter for an operational semantics for the STG machine."
url="http://hackage.haskell.org/package/ministg"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-containers' 'haskell-directory' 'haskell-filepath' 'haskell-haskell98' 'haskell-monads-tf' 'haskell-parsec>=3' 'haskell-pretty' 'haskell-transformers' 'haskell-xhtml')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/ministg/0.2/ministg-0.2.tar.gz)
md5sums=('f12d110374077c44f38b92088b312c3e')
build() {
    cd ${srcdir}/ministg-0.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
