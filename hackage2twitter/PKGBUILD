# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hackage2twitter
pkgrel=2
pkgver=0.2.1
pkgdesc="Send new Hackage releases to Twitter"
url="http://hackage.haskell.org/package/hackage2twitter"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-feed>=0.3.6' 'haskell-feed2twitter>=0.2')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/hackage2twitter/0.2.1/hackage2twitter-0.2.1.tar.gz)
md5sums=('1e5748143c5039e0c30809f18a5969d9')
build() {
    cd ${srcdir}/hackage2twitter-0.2.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
