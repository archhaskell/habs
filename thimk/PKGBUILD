# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=thimk
pkgrel=1
pkgver=0.3.1
pkgdesc="Command-line spelling word suggestion tool"
url="http://hackage.haskell.org/package/thimk"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-edit-distance>=0.1' 'haskell-parseargs' 'haskell-phonetic-code' 'haskell-sqlite')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/thimk/0.3.1/thimk-0.3.1.tar.gz)
md5sums=('a6bfd0bfb3bfae6dc810c7627591a0b5')
build() {
    cd ${srcdir}/thimk-0.3.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 COPYING ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
