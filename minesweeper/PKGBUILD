# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=minesweeper
pkgrel=2
pkgver=0.8.8.2
pkgdesc="Minesweeper game which is always solvable without guessing"
url="http://hackage.haskell.org/package/minesweeper"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'gtk2hs' 'haskell-binary' 'haskell-containers' 'derive' 'haskell-directory' 'haskell-filepath' 'haskell-random' 'haskell-time')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/minesweeper/0.8.8.2/minesweeper-0.8.8.2.tar.gz)
md5sums=('66d4baed0c488978740b119947a9ed7e')
build() {
    cd ${srcdir}/minesweeper-0.8.8.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
