Index: habs/cabal-install.cabal
===================================================================
--- habs.orig/cabal-install.cabal
+++ habs/cabal-install.cabal
@@ -123,6 +123,7 @@ executable cabal
         HTTP       >= 4000.0.8 && < 4001,
         mtl        >= 2.0      && < 3,
         network    >= 1        && < 3,
+        network-uri,
         pretty     >= 1        && < 1.2,
         random     >= 1        && < 1.1,
         stm        >= 2.0      && < 3,
