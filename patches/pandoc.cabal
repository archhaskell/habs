Index: habs/pandoc.cabal
===================================================================
--- habs.orig/pandoc.cabal
+++ habs/pandoc.cabal
@@ -253,7 +253,7 @@ Library
   else
      Build-Depends: network >= 2 && < 2.6
   if flag(https)
-     Build-Depends: http-client >= 0.3.2 && < 0.4,
+     Build-Depends: http-client >= 0.3.2 && < 0.5,
                     http-client-tls >= 0.2 && < 0.3,
                     http-types >= 0.8 && < 0.9
      cpp-options:   -DHTTP_CLIENT
