# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hashell
pkgrel=2
pkgver=1.0
pkgdesc="Simple shell written in Haskell"
url="http://hackage.haskell.org/package/Hashell"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-directory' 'haskell-haskell98' 'haskell-parsec' 'haskell-process' 'haskell-readline' 'haskell-regex-compat' 'haskell-unix')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/Hashell/1.0/Hashell-1.0.tar.gz)
md5sums=('cc5564dc11f2343e54eb52e209dcb470')
build() {
    cd ${srcdir}/Hashell-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
