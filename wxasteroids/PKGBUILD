# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=wxasteroids
pkgrel=2
pkgver=1.0
pkgdesc="Try to avoid the asteroids with your space ship"
url="http://hackage.haskell.org/package/wxAsteroids"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-directory' 'haskell-random' 'haskell-wx' 'haskell-wxcore')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/wxAsteroids/1.0/wxAsteroids-1.0.tar.gz)
md5sums=('bfd7b1694c77ccf7460198537d417da0')
build() {
    cd ${srcdir}/wxAsteroids-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE.txt ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
