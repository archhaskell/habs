# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=bluetile
pkgrel=1
pkgver=0.5.3
pkgdesc="full-featured tiling for the GNOME desktop environment"
url="http://hackage.haskell.org/package/bluetile"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-configfile' 'haskell-x11>=1.4' 'haskell-x11-xft' 'haskell-containers' 'haskell-directory' 'haskell-filepath' 'haskell-glade' 'haskell-gtk' 'haskell-mtl' 'haskell-process' 'haskell-random' 'haskell-regex-compat' 'haskell-unix' 'haskell-utf8-string' 'xmonad>=0.9.1' 'xmonad-contrib>=0.9.1')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/bluetile/0.5.3/bluetile-0.5.3.tar.gz)
md5sums=('6c3aecd280640f8435a8400e102cae95')
build() {
    cd ${srcdir}/bluetile-0.5.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
