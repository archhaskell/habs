# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=astrds
pkgrel=2
pkgver=0.1
pkgdesc="an incomplete 2d space game"
url="http://hackage.haskell.org/package/astrds"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-monadrandom' 'haskell-opengl' 'haskell-sdl' 'haskell-sdl-image' 'haskell-sdl-mixer' 'haskell-sdl-ttf' 'haskell-containers' 'haskell-directory' 'haskell-mtl' 'haskell-random' 'haskell-unix')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/astrds/0.1/astrds-0.1.tar.gz)
md5sums=('fb8e5f8a87a372789cf120aafedd311b')
build() {
    cd ${srcdir}/astrds-0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
