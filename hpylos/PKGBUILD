# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hpylos
pkgrel=2
pkgver=1.0
pkgdesc="AI of Pylos game with GLUT interface."
url="http://hackage.haskell.org/package/hpylos"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-eeconfig' 'haskell-glut' 'haskell-opengl' 'haskell-array' 'haskell-containers')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/hpylos/1.0/hpylos-1.0.tar.gz)
md5sums=('3542b6469651849e95b88ef38dae3153')
build() {
    cd ${srcdir}/hpylos-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
