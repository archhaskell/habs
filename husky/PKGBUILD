# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=husky
pkgrel=2
pkgver=0.4
pkgdesc="A simple command line calculator."
url="http://hackage.haskell.org/package/husky"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-containers>=0.1.0.0' 'haskell-mtl>=1.1.0.0' 'haskell-old-locale>=1.0.0.0' 'haskell-parsec' 'haskell-readline>=1.0.1.0' 'haskell-time>=1.0.0.0')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/husky/0.4/husky-0.4.tar.gz)
md5sums=('ccb7b05cc3e11d495f5b9fa26396f87e')
build() {
    cd ${srcdir}/husky-0.4
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
