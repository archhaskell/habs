# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=yampasynth
pkgrel=2
pkgver=0.0.1
pkgdesc="Software synthesizer"
url="http://hackage.haskell.org/package/YampaSynth"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'gtk2hs' 'haskell-hcodecs' 'haskell-openal' 'haskell-yampa' 'haskell-array' 'haskell-bytestring' 'haskell-containers')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/YampaSynth/0.0.1/YampaSynth-0.0.1.tar.gz)
md5sums=('f860d99c070fd6a2f263f1c097b0d3d7')
build() {
    cd ${srcdir}/YampaSynth-0.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
