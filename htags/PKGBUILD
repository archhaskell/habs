# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=htags
pkgrel=2
pkgver=1.0.1
pkgdesc="A Haskell98 parsing tags program similar to ctags."
url="http://hackage.haskell.org/package/htags"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-directory' 'haskell-filepath' 'haskell-haskell-src' 'haskell-mtl')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/htags/1.0.1/htags-1.0.1.tar.gz)
md5sums=('e93f3339d3c062ca5a572dffdd4ab531')
build() {
    cd ${srcdir}/htags-1.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
