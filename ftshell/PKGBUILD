# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=ftshell
pkgrel=2
pkgver=0.3
pkgdesc="Shell interface to the FreeTheorems library."
url="http://hackage.haskell.org/package/ftshell"
license=('custom:PublicDomain')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-shellac>=0.9' 'haskell-shellac-readline>=0.9' 'haskell-containers>=0.1.0.1' 'haskell-free-theorems>=0.3' 'haskell-mtl>=1.0' 'haskell-pretty>=1.0.0.0')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/ftshell/0.3/ftshell-0.3.tar.gz)
md5sums=('1d57ad3ce7860fa3a37f6da18f631975')
build() {
    cd ${srcdir}/ftshell-0.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
