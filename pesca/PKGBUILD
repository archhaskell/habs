# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=pesca
pkgrel=2
pkgver=4.0.1
pkgdesc="Proof Editor for Sequent Calculus"
url="http://hackage.haskell.org/package/pesca"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-process')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/pesca/4.0.1/pesca-4.0.1.tar.gz)
md5sums=('af6d3124524c0b99a7972ced001a57b9')
build() {
    cd ${srcdir}/pesca-4.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
