# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=datapacker
pkgrel=2
pkgver=1.0.1
pkgdesc="Tool to help pack files into the minimum number of CDs/DVDs/etc"
url="http://hackage.haskell.org/package/datapacker"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-missingh>=1.0.1' 'haskell-directory' 'haskell-filepath' 'haskell-hslogger' 'haskell-mtl' 'haskell-process' 'haskell-unix')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/datapacker/1.0.1/datapacker-1.0.1.tar.gz)
md5sums=('5e5fcc35f9b7355e525f5112683c6149')
build() {
    cd ${srcdir}/datapacker-1.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
