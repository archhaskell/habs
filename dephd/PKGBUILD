# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=dephd
pkgrel=2
pkgver=0.1.3
pkgdesc="Analyze quality of nucleotide sequences."
url="http://hackage.haskell.org/package/dephd"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-bio>=0.4' 'haskell-bytestring' 'haskell-directory' 'haskell-process' 'haskell-regex-compat')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/dephd/0.1.3/dephd-0.1.3.tar.gz)
md5sums=('730e3b97d5914e368cba58c695b5e160')
build() {
    cd ${srcdir}/dephd-0.1.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
