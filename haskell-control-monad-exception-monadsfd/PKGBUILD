# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=haskell-control-monad-exception-monadsfd
pkgrel=1
pkgver=0.8.0.3
pkgdesc="Monads-fd instances for the EMT exceptions monad transformer"
url="http://hackage.haskell.org/package/control-monad-exception-monadsfd"
license=('custom:PublicDomain')
arch=('i686' 'x86_64')
makedepends=()
depends=('ghc' 'haskell-cabal' 'haskell-control-monad-exception>=0.8.0' 'haskell-monads-fd>=0.0.0.1' 'haskell-transformers>=0.1.0')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/control-monad-exception-monadsfd/0.8.0.3/control-monad-exception-monadsfd-0.8.0.3.tar.gz)
install=haskell-control-monad-exception-monadsfd.install
md5sums=('87205f318405c250bbba40cb84d239e6')
build() {
    cd ${srcdir}/control-monad-exception-monadsfd-0.8.0.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup haddock || return 1
    runhaskell Setup register   --gen-script || return 1
    runhaskell Setup unregister --gen-script || return 1
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/$pkgname/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/$pkgname/unregister.sh
    install -d -m755 $pkgdir/usr/share/doc/ghc/html/libraries
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/control-monad-exception-monadsfd
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
