# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=topkata
pkgrel=2
pkgver=0.2.3
pkgdesc="OpenGL Arcade Game"
url="http://hackage.haskell.org/package/topkata"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-alut' 'haskell-glut' 'haskell-openal' 'haskell-opengl' 'haskell-array' 'haskell-filepath' 'haskell-random')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/topkata/0.2.3/topkata-0.2.3.tar.gz)
md5sums=('37d25f677f70c875d4d2dabf2b6ab6c5')
build() {
    cd ${srcdir}/topkata-0.2.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
