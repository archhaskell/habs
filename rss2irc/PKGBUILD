# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=rss2irc
pkgrel=2
pkgver=0.4.2
pkgdesc="watches an RSS/Atom feed and writes it to an IRC channel"
url="http://hackage.haskell.org/package/rss2irc"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-http' 'haskell-extensible-exceptions' 'haskell-feed' 'haskell-haskell98' 'haskell-irc' 'haskell-mtl' 'haskell-network' 'haskell-parallel' 'haskell-regexpr' 'haskell-split' 'haskell-strict-concurrency' 'haskell-tagsoup' 'haskell-time')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/rss2irc/0.4.2/rss2irc-0.4.2.tar.gz)
md5sums=('3560f5fdb1375b55d7e9fa5754da80ce')
build() {
    cd ${srcdir}/rss2irc-0.4.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
