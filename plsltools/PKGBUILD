# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=plsltools
pkgrel=2
pkgver=0.0.2
pkgdesc="So far just a lint like program for PL/SQL. Diff and refactoring tools are planned"
url="http://hackage.haskell.org/package/PlslTools"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array>=0.1.0.0' 'haskell-directory>=1.0.0.1' 'haskell-filepath>=1.1.0.0' 'haskell-haskell98' 'haskell-old-locale>=1.0.0.0' 'haskell-old-time>=1.0.0.0' 'haskell-parsec>=2.1.0.1' 'haskell-process>=1.0.0.1' 'haskell-random>=1.0.0.0')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/PlslTools/0.0.2/PlslTools-0.0.2.tar.gz)
md5sums=('b6ea1eab56f3245c167eee5be818d8ea')
build() {
    cd ${srcdir}/PlslTools-0.0.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
