# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=xsact
pkgrel=1
pkgver=1.7.3
pkgdesc="Cluster EST sequences"
url="http://hackage.haskell.org/package/xsact"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array' 'haskell-containers' 'haskell-directory' 'haskell-process' 'haskell-random' 'haskell-unix')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/xsact/1.7.3/xsact-1.7.3.tar.gz)
md5sums=('ed6cfe64da6d81c19349b26918eb7c06')
build() {
    cd ${srcdir}/xsact-1.7.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
