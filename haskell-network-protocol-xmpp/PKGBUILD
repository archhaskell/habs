# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=haskell-network-protocol-xmpp
pkgrel=1
pkgver=0.3.2.2
pkgdesc="Client <-> Server communication over XMPP"
url="http://hackage.haskell.org/package/network-protocol-xmpp"
license=('GPL-3')
arch=('i686' 'x86_64')
makedepends=()
depends=('ghc' 'haskell-cabal' 'haskell-bytestring' 'haskell-gnuidn' 'haskell-gnutls' 'haskell-gsasl' 'haskell-libxml-sax' 'haskell-monads-tf' 'haskell-network' 'haskell-text' 'haskell-transformers' 'haskell-xml-types')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/network-protocol-xmpp/0.3.2.2/network-protocol-xmpp-0.3.2.2.tar.gz)
install=haskell-network-protocol-xmpp.install
md5sums=('9f0a15341f00ac7819fd83c7a8e0c2a2')
build() {
    cd ${srcdir}/network-protocol-xmpp-0.3.2.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup haddock || return 1
    runhaskell Setup register   --gen-script || return 1
    runhaskell Setup unregister --gen-script || return 1
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/$pkgname/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/$pkgname/unregister.sh
    install -d -m755 $pkgdir/usr/share/doc/ghc/html/libraries
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/network-protocol-xmpp
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
