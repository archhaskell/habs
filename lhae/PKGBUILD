# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=lhae
pkgrel=1
pkgver=0.0.3
pkgdesc="Simple spreadsheet program"
url="http://hackage.haskell.org/package/lhae"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array' 'haskell-containers' 'haskell-directory' 'haskell-filepath' 'haskell-hgettext' 'haskell-mtl' 'haskell-parsec' 'haskell-process' 'haskell-setlocale' 'haskell-utf8-string' 'haskell-wx' 'haskell-wxcore')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/lhae/0.0.3/lhae-0.0.3.tar.gz)
md5sums=('4f2e1c2e26241bb0fb5afc09c61d83e3')
build() {
    cd ${srcdir}/lhae-0.0.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
