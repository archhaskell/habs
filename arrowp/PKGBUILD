# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=arrowp
pkgrel=2
pkgver=0.5.0.1
pkgdesc="preprocessor translating arrow notation into Haskell 98"
url="http://hackage.haskell.org/package/arrowp"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array' 'haskell-containers' 'haskell-haskell-src')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/arrowp/0.5.0.1/arrowp-0.5.0.1.tar.gz)
md5sums=('1b5edebfbd402b4eb79b12fb07899b5a')
build() {
    cd ${srcdir}/arrowp-0.5.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
