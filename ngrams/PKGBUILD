# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=ngrams
pkgrel=2
pkgver=1.1
pkgdesc="Simple application for calculating n-grams using Google."
url="http://hackage.haskell.org/package/NGrams"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-hcl>=1.2' 'haskell-http>=3000.0.0' 'haskell-network>=2.0.0.0' 'haskell-regex-compat>=0.7.0.0')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/NGrams/1.1/NGrams-1.1.tar.gz)
md5sums=('387bb4e17ce8df01baa85e24f4576b06')
build() {
    cd ${srcdir}/NGrams-1.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
