# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hask-home
pkgrel=2
pkgver=2009.3.18
pkgdesc="Generate homepages for cabal packages"
url="http://hackage.haskell.org/package/hask-home"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-cabal>=1.6' 'haskell-directory' 'haskell-hmarkup>=3000.0.1' 'haskell-hsemail' 'haskell-network' 'haskell-parsec' 'haskell-process' 'haskell-regex-compat' 'haskell-xhtml>=3000.0.2.1')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/hask-home/2009.3.18/hask-home-2009.3.18.tar.gz)
md5sums=('e8ba47abbe9de56510f7f90fb153698f')
build() {
    cd ${srcdir}/hask-home-2009.3.18
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
