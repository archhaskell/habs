# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=omnicodec
pkgrel=1
pkgver=0.4.0
pkgdesc="data encoding and decoding command line utilities"
url="http://hackage.haskell.org/package/omnicodec"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-bytestring' 'haskell-dataenc>=0.13' 'haskell-directory' 'haskell-filepath' 'haskell-haskell98')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/omnicodec/0.4.0/omnicodec-0.4.0.tar.gz)
md5sums=('a1c0661045d1a8a62e96696747f94124')
build() {
    cd ${srcdir}/omnicodec-0.4.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
