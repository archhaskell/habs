# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=lambdashell
pkgrel=2
pkgver=0.9.1
pkgdesc="Simple shell for evaluating lambda expressions"
url="http://hackage.haskell.org/package/LambdaShell"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-shellac>=0.9' 'haskell-shellac-readline>=0.9' 'haskell-containers' 'haskell-haskell98' 'haskell-mtl' 'haskell-parsec' 'haskell-readline' 'readline')
depends=('gmp' 'readline')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/LambdaShell/0.9.1/LambdaShell-0.9.1.tar.gz)
md5sums=('fc38d26dfb7800ccd9ce47fe4f29f595')
build() {
    cd ${srcdir}/LambdaShell-0.9.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
