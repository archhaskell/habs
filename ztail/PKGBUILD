# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=ztail
pkgrel=2
pkgver=1.0
pkgdesc="Multi-file, colored, filtered log tailer."
url="http://hackage.haskell.org/package/ztail"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array' 'haskell-hinotify' 'haskell-old-locale' 'haskell-process' 'haskell-regex-compat' 'haskell-time' 'haskell-unix')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/ztail/1.0/ztail-1.0.tar.gz)
md5sums=('868f55196b6430bfccd70f5d9aa09bfa')
build() {
    cd ${srcdir}/ztail-1.0
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
