# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=frag
pkgrel=2
pkgver=1.1.2
pkgdesc="A 3-D First Person Shooter Game"
url="http://hackage.haskell.org/package/frag"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-glut' 'haskell-opengl>=2.0' 'haskell-array' 'haskell-random')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/frag/1.1.2/frag-1.1.2.tar.gz)
md5sums=('379bc7f850e1a1177651a35af6f15b63')
build() {
    cd ${srcdir}/frag-1.1.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
