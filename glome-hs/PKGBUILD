# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.6.1
pkgname=glome-hs
pkgrel=1
pkgver=0.51
pkgdesc="ray tracer"
url="http://hackage.haskell.org/package/glome-hs"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-glut' 'haskell-opengl' 'haskell-time')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/glome-hs/0.51/glome-hs-0.51.tar.gz)
md5sums=('2a2c6a4aba051148f93f046d4cdbfc23')
build() {
    cd ${srcdir}/glome-hs-0.51
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
