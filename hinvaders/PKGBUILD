# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=hinvaders
pkgrel=2
pkgver=0.1
pkgdesc="Space Invaders"
url="http://hackage.haskell.org/package/hinvaders"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-haskell98' 'haskell-random')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/hinvaders/0.1/hinvaders-0.1.tar.gz)
md5sums=('e9e6cb3fd7c74cdcd263462cdbe6f68b')
build() {
    cd ${srcdir}/hinvaders-0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
