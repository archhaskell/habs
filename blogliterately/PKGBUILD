# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.2
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=blogliterately
pkgrel=1
pkgver=0.3
pkgdesc="A tool for posting Haskelly articles to blogs"
url="http://hackage.haskell.org/package/BlogLiterately"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-haxml' 'haskell-cmdargs' 'haskell-haskell98' 'haskell-haxr' 'hscolour' 'haskell-pandoc' 'haskell-parsec' 'haskell-utf8-string' 'haskell-xhtml')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/BlogLiterately/0.3/BlogLiterately-0.3.tar.gz)
md5sums=('bcb77e8d5ba55b9e2523d4b0253b66ff')
build() {
    cd ${srcdir}/BlogLiterately-0.3
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
