# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=pxsl-tools
pkgrel=2
pkgver=1.0.1
pkgdesc="Parsimonious XML Shorthand Language--to-XML compiler"
url="http://hackage.haskell.org/package/pxsl-tools"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-containers' 'haskell-mtl>=1.0' 'haskell-parsec>=2.0')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/pxsl-tools/1.0.1/pxsl-tools-1.0.1.tar.gz)
md5sums=('7081065a40094174c85411d6a44eb829')
build() {
    cd ${srcdir}/pxsl-tools-1.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
