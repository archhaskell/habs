# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7.3
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
# Keep up to date on http://archhaskell.wordpress.com/
#
pkgname=babylon
pkgrel=1
pkgver=0.2
pkgdesc="An implementation of a simple 2-player board game"
url="http://hackage.haskell.org/package/babylon"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-array' 'haskell-containers' 'haskell-haskell98' 'haskell-wx>=0.11' 'haskell-wxcore>=0.11')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/babylon/0.2/babylon-0.2.tar.gz)
md5sums=('13a53821b1ebcc0d26afb0e9736f1179')
build() {
    cd ${srcdir}/babylon-0.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
