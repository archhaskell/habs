# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=vintage-basic
pkgrel=2
pkgver=1.0.1
pkgdesc="Interpreter for microcomputer-era BASIC"
url="http://hackage.haskell.org/package/vintage-basic"
license=('custom:BSD3')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-hunit>=1.2' 'haskell-array>=0.1' 'haskell-directory>=1' 'haskell-filepath>=1.1' 'haskell-mtl>=1.1' 'haskell-parsec>=2.1' 'haskell-process>=1' 'haskell-random>=1' 'haskell-regex-base>=0.72' 'haskell-regex-posix>=0.72' 'haskell-time>=1.1')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/vintage-basic/1.0.1/vintage-basic-1.0.1.tar.gz)
md5sums=('1f357810e193401607b4f387db1a1e46')
build() {
    cd ${srcdir}/vintage-basic-1.0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
    install -D -m644 LICENSE.txt ${pkgdir}/usr/share/licenses/$pkgname/LICENSE || return 1
    rm -f ${pkgdir}/usr/share/doc/${pkgname}/LICENSE
}
