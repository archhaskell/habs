# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=linkchk
pkgrel=2
pkgver=0.0.2
pkgdesc="linkchk is a network interface link ping monitor."
url="http://hackage.haskell.org/package/linkchk"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'gtk2hs' 'haskell-haskell98' 'haskell-popenhs' 'haskell-regex-compat' 'haskell-unix')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/linkchk/0.0.2/linkchk-0.0.2.tar.gz)
md5sums=('5ca3fbaa5e1f69edd72fdd54540d36cc')
build() {
    cd ${srcdir}/linkchk-0.0.2
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
