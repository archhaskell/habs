# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.7
# Note: we list all package dependencies.
# Your package tool should understand 'provides' syntax
#
pkgname=bloxorz
pkgrel=2
pkgver=0.1
pkgdesc="OpenGL Logic Game"
url="http://hackage.haskell.org/package/bloxorz"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=('ghc' 'haskell-cabal' 'haskell-glfw' 'haskell-opengl')
depends=('gmp')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/bloxorz/0.1/bloxorz-0.1.tar.gz)
md5sums=('1c328d21725a66934dbd6c5a39faec7b')
build() {
    cd ${srcdir}/bloxorz-0.1
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} || return 1
    runhaskell Setup build                   || return 1
    runhaskell Setup copy --destdir=${pkgdir} || return 1
}
