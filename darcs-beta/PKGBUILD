# Maintainer: Arch Haskell Team <arch-haskell@haskell.org>
_hkgname=darcs-beta
pkgname=darcs-beta
pkgver=2.4.99.1
pkgrel=2
pkgdesc="a distributed, interactive, smart revision control system"
url="http://hackage.haskell.org/package/${_hkgname}"
license=('GPL')
arch=('i686' 'x86_64')
makedepends=()
depends=('gmp' 'curl' 'ghc' 'haskell-http<4000.1' 'haskell-array<0.4' 'haskell-bytestring<0.10' 'haskell-containers<0.4' 'haskell-directory<1.1' 'haskell-extensible-exceptions<0.2' 'haskell-filepath<1.2' 'haskell-hashed-storage<0.6' 'haskell-haskeline<0.7' 'haskell-html<1.1' 'haskell-mmap<0.6' 'haskell-mtl<1.2' 'haskell-network<2.3' 'haskell-old-time<1.1' 'haskell-parsec<3.1' 'haskell-process<1.1' 'haskell-random<1.1' 'haskell-regex-compat<0.94' 'haskell-tar<0.4' 'haskell-terminfo<0.4' 'haskell-text>=0.3' 'haskell-unix<2.5' 'haskell-zlib<0.6.0.0' 'curl')
options=('strip')
source=(http://hackage.haskell.org/packages/archive/${_hkgname}/${pkgver}/${_hkgname}-${pkgver}.tar.gz)
install=${pkgname}.install
build() {
    cd ${srcdir}/${_hkgname}-${pkgver}
    runhaskell Setup configure --prefix=/usr --docdir=/usr/share/doc/${pkgname} -O --enable-split-objs --enable-shared
    runhaskell Setup build
    runhaskell Setup haddock
    runhaskell Setup register   --gen-script
    runhaskell Setup unregister --gen-script
    sed -i -r -e "s|ghc-pkg.*unregister[^ ]* |&'--force' |" unregister.sh
}
package() {
    cd ${srcdir}/${_hkgname}-${pkgver}
    install -D -m744 register.sh   ${pkgdir}/usr/share/haskell/${pkgname}/register.sh
    install    -m744 unregister.sh ${pkgdir}/usr/share/haskell/${pkgname}/unregister.sh
    install -d -m755 ${pkgdir}/usr/share/doc/ghc/html/libraries
    ln -s /usr/share/doc/${pkgname}/html ${pkgdir}/usr/share/doc/ghc/html/libraries/${_hkgname}
    runhaskell Setup copy --destdir=${pkgdir}
}
md5sums=('3f749e3379827a7bb6697e8a72de21fa')
